<Window x:Class="_2025._03._14._WPF_alapok3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_2025._03._14._WPF_alapok3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="43*"/>
            <RowDefinition Height="153*"/>
            <RowDefinition Height="238*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="235*"/>
            <ColumnDefinition Width="321*"/>
            <ColumnDefinition Width="245*"/>
         
        </Grid.ColumnDefinitions>

        <Button Grid.Column="3" Grid.Row="2" Content="gomb"/>

        <ListBox>
            <ListBoxItem>tartalom</ListBoxItem>
        </ListBox>

        <!--<DockPanel Grid.ColumnSpan="2" HorizontalAlignment="Left" Width="512">
            <WrapPanel DockPanel.Dock="Top" Height="20">
                <Button>menu1</Button>
                <Button>menu2</Button>
                <Button>menu3</Button>
                <Button>menu4</Button>
            </WrapPanel>
            <StackPanel DockPanel.Dock="Left" Width="106">
                <Button>menu1</Button>
                <Button>menu2</Button>
                <Button>menu3</Button>
                <Button>menu4</Button>
            </StackPanel>
            <DockPanel DockPanel.Dock="Bottom">
                <Button Height="35">lábléc</Button>
            </DockPanel>
            <DockPanel DockPanel.Dock="Right" Width="125">
                <Button DockPanel.Dock="top">alma</Button>
                <Button DockPanel.Dock="Top">alma</Button>
                <Button DockPanel.Dock="Top">alma</Button>
                <Button DockPanel.Dock="Top" Width="124">alma</Button>
            </DockPanel>
            <ListBox DockPanel.Dock="Right">gom5</ListBox>
        </DockPanel>-->

    </Grid>
</Window>
